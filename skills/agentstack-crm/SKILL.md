---
name: agentstack-crm
description: CRM contacts, deals, pipeline — use crm.* MCP actions.
---

# CRM

Mirror of Cursor `agentstack-crm`: `crm.upsert_contact`, `crm.create_deal`, `crm.move_deal_stage`, `crm.get_contact_360`. Live catalog: `GET /mcp/actions`.